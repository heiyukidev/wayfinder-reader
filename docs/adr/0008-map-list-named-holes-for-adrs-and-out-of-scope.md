# Map list named holes for ADRs and out-of-scope records

The **Map list** is the desk for tracker documents plus the durable files Matt Pocock skills write at known paths: language files (ADR 0006), **ADRs** (`docs/adr/*.md`, and a mapped context’s `docs/adr/` when a CONTEXT-MAP exists), and **Out-of-scope records** (`.out-of-scope/*.md`). Those project-level rows are never **Finished** and never **Archived** by the Effort control.

This amends ADR 0002’s containment with two named holes, like language. Rejected: walking the Project for any markdown a skill might write (`workflows/`, questionnaires, `PRODUCT.md`, `scripts/`, source, `.env`).

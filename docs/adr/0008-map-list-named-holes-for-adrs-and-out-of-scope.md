# Map list named holes for ADRs and out-of-scope records

The **Map list** is the desk for tracker documents plus the durable files Matt Pocock skills write at known paths: language files (ADR 0006, discovery in [ADR 0010](0010-marker-walk-finds-sites.md)), **ADRs** (each **Site**’s `docs/adr/*.md`), and **Out-of-scope records** (each Site’s `.out-of-scope/*.md`). Those rows are never **Finished** and never **Archived** by the Effort control.

This amends ADR 0002’s containment with two named holes, like language. Rejected: walking the Project for any markdown a skill might write (`workflows/`, questionnaires, `PRODUCT.md`, `scripts/`, source, `.env`).

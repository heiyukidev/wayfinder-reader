# What keyboard shortcut opens Paste preview

Type: grilling
Blocked by:

## Question

The caption Paste control is in `public/`. Which keyboard shortcut should open (and toggle) **Paste preview**?

It stays disabled until a **Project** is Loaded. It must not hijack global paste (`Cmd+V` / `navigator.clipboard.readText`). Compose vs Show and restore-after-Map-list-click stay as shipped.

## Comments
